# real configs hold private keys — never commit; only *.example.json templates are tracked
config*.json
!config*.example.json

.venv/
__pycache__/
*.log

# sub-account keystore holds private keys
accounts*.json
